import React from 'react';

import {
  StyleSheet,
  View,
} from 'react-native';
import GeoLoggerScreen from './src/GeoLoggerScreen';

const App = () => {

  return (
    <GeoLoggerScreen />
  );
};

const styles = StyleSheet.create({
  container: {
  },
});

export default App;
